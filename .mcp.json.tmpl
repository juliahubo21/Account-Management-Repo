{
  "mcpServers": {
    "affinity-mcp": {
      "type": "stdio",
      "command": "uvx",
      "args": ["affinity-mcp"],
      "env": {
        "AFFINITY_API_KEY": "${AFFINITY_API_KEY}"
      }
    }
  }
}
